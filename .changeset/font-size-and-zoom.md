---
"helmor": patch
---

Add Code font size and Interface zoom to Appearance settings:
- Code font size (10–20 px) controls AI message code blocks independently from chat body, mirrored to the new `--code-font-size` CSS var.
- Interface zoom (50–200 %) exposes the existing webview zoom as a stepper so users can scale the entire UI (sidebar, panels, dialogs).
